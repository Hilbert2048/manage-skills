interface:
  display_name: "Manage Skills"
  short_description: "Install and organize shared skills across coding agents"
  default_prompt: "Use $manage-skills to install, update, link, or migrate a reusable coding-agent skill."
policy:
  allow_implicit_invocation: true
