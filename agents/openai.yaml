interface:
  display_name: "Manage Skills"
  short_description: "AI-assisted shared skill installs across coding agents"
  default_prompt: "Use $manage-skills to install, update, link, or migrate a reusable coding-agent skill in the shared hub."
policy:
  allow_implicit_invocation: true
